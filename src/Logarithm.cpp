#include "../include/Logarithm.h"
#include <math.h>
int Log2(int x)
{
  logar = log(x) / log(2)
  if (logar == int logar) 
  {
		return logar;
  }
  elif (x <= 0)
  {
		return -2;
   }
	else 
  {
		return -1;
  }
}
