#include "../include/Logarithm.h"

#include <gtest/gtest.h>
TEST (CheckingLog2, eight){
	ASSERT_DOUBLE_EQ(log2(8), 3);
}
TEST (CheckingLog2, nine){
	ASSERT_DOUBLE_EQ(log2(8), -2);
}
TEST (CheckingLog2, zero){
	ASSERT_DOUBLE_EQ(result(log2(8), -1;
}
